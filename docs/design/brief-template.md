# Brand Kit / Design Brief — Template

For Amal to fill in once. This is the single handover that unblocks the Designer agent and the theme build (`docs/setup-checklist.md`).

## 1. Brand basics

- **Brand name (final):**
- **Tagline (if any):**
- **Logo file(s):** (vector — .ai/.svg preferred, plus a PNG export; both a full logo and an icon/favicon-only mark)
- **Launch market(s):** (which GCC country(ies) first — also feeds `docs/dispatch/courier-matrix.md`)

## 2. Color palette

| Role | Hex | Notes |
|---|---|---|
| Primary | | Main brand color — buttons, links, key accents |
| Secondary | | Supporting accent |
| Neutral / background | | Page background, cards |
| Text | | Body copy — check contrast against background |
| Success / Error / Warning | | Cart, form validation states |

## 3. Typography

- **Arabic typeface preference (if any):**
- **Latin/English typeface preference (if any):**
- Fallback: if none specified, the Designer agent proposes an RTL-tested pairing for approval.

## 4. Tone & voice

- 3–5 adjectives describing how the brand should sound (e.g. "warm, confident, minimal, premium, not salesy"):
- Any words/phrases to avoid:
- Reference brands/sites whose voice or look you like (for direction, not copying):

## 5. Product photography style

- Studio white background / lifestyle / both:
- Any existing product photos to reuse, or all photography still pending supplier sourcing (`docs/setup-checklist.md`)?

## 6. RTL-specific notes

- Any Arabic marketing copy conventions already in use (formality level, dialect vs. MSA) — this also feeds the SEO Content Writer agent's voice.

---

Once this is filled in, the Designer agent turns `docs/design/wireframes.md` into real `theme/sections/` files against these values, and the SEO Content Writer agent switches from placeholder to on-brand voice.
